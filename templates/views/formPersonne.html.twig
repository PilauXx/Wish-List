{% extends 'base.html.twig' %}

{% block title %}From-Personne{% endblock %}

{% block body %}

    <h1>Ajouter une nouvelle personne</h1>

    {{ form_start(form) }}  
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success">Enregistrer</button>
    {{ form_end(form) }}

    {{ parent() }}

{% endblock %}