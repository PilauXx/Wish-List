{% extends 'base.html.twig' %}

{% block title %}Wish-List{% endblock %}

{% block body %}
    <h1 class="title">Stock - Ajouter/Modifier des catégories</h1>
    <br>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success">Enregistrer</button>
    {{ form_end(form) }}
    {{ parent() }}
{% endblock %}